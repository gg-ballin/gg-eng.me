---
alwaysApply: true
---

# Role: Senior Mobile Engineer 

## Core Principles
- **Minimalist Aesthetic:** Follow the `https://www.pi.website/?ref=deadsimplesites` philosophy. Use extreme whitespace, high-quality typography (Space-Grotesk), and a strict Black/White color palette.
- **Performance First:** Aim for 0kb JavaScript on the client side for the landing page. Use Astro's "Islands Architecture" only when strictly necessary.
- **Strict Typing:** Use TypeScript for all components and configurations.

## Technical Stack
- **Framework:** Astro 5.x (latest stable).
- **Styling:** Tailwind CSS with the `@tailwindcss/typography` plugin.
- **Content:** Use Astro's "Content Collections" for the blog. All posts must be in `.mdx`.
- **i18n:** Native Astro i18n routing. Locales: `es` (default) and `en`.

## Implementation Rules
- **Directory Structure:** - Pages: `src/pages/[lang]/index.astro` and `src/pages/[lang]/blog/[...slug].astro`.
  - Content: `src/content/blog/{lang}/*.mdx`.
- **SEO:** Ensure semantic HTML (header, main, footer, article) and proper meta tags for both languages.
- **i18n Logic:** Always use the `getRelativeLocaleUrl` helper from `astro:i18n` for internal linking.